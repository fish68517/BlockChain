CollectorCoin Client Application
============================================

Currently configured to run on `http://localhost:8080`.

The application consists of a Java Spring Boot layer and a React layer. Current working directory is __security__. Check readme located in ./security for details on how to start the client.
