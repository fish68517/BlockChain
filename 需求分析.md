### 作业：2. 去中心化课程资源共享平台  
要求：构建一个利用区块链的课程资料共享系统，通过通证激励用户贡
献优质资源，确保版权追溯与公平分配。  
实现：利用智能合约管理资源访问权限，保护用户隐私。  

好的，我们来分析一下这个作业需求，并设计一个最简单的实现方案。

### 1. 需求分析

根据图片内容，我们需要构建一个系统，核心要求如下：

*   **目标**：去中心化课程资源共享平台。
*   **核心技术**：使用区块链。
*   **关键功能**：
    1.  **激励机制**：通过通证（Token）激励用户贡献优质资源。
    2.  **版权追溯**：确保资源上传和使用的记录不可篡改，可以追溯到作者。
    3.  **公平分配**：资源的价值（如访问收益）能够公平地分配给贡献者。
    4.  **权限管理**：利用智能合约管理谁可以访问哪些资源。
    5.  **隐私保护**：保护用户的个人隐私。

### 2. 技术选型分析

在 `Go`、`Rust` 和 `Solidity` 三者中，我们可以这样分工：

*   **Solidity**：这是编写以太坊智能合约的首选语言。由于作业的核心要求是利用智能合约管理权限和分配，Solidity 是**不可或C缺**的。它将负责平台最核心的链上逻辑。
*   **Go / Rust**：这两种语言通常用于构建区块链本身或与之交互的后端服务。对于一个“最简单”的作业方案，我们可以暂时不开发复杂的后端，而是假设所有交互都通过简单的Web前端和钱包插件（如MetaMask）直接与智能合约进行。

**结论**：为了实现最简单的方案，我们应该**重点设计 Solidity 智能合约**，因为它是满足所有核心要求的关键。

### 3. 最简化设计方案

我们将系统分为两部分：**链上（On-Chain）**和**链下（Off-Chain）**。

*   **链上 (Solidity 智能合约)**：处理核心逻辑、所有权、权限和价值转移。
*   **链下 (去中心化存储)**：存储实际的课程文件（如视频、文档），因为将大文件存储在区块链上极其昂贵且效率低下。最常用的方案是 **IPFS** (星际文件系统)。

---

#### **方案详细设计**

**第一步：资源处理（链下）**

1.  **上传文件**：用户想要分享一个课程文件（如 `course.pdf`）。
2.  **加密文件（实现隐私保护）**：在上传到公共存储之前，系统先对这个文件进行加密。只有拥有密钥的人才能解密查看。
3.  **存入 IPFS**：将**加密后**的文件上传到 IPFS。IPFS会返回一个唯一的哈希值（例如 `QmX...`），这个哈希值就是该文件在网络上的地址。

**第二步：信息上链（链上 Solidity 智能合约）**

用户调用智能合约的一个功能，将课程信息记录在区块链上。智能合约需要存储以下信息：

*   **资源信息**：
    *   资源ID（唯一编号）
    *   资源名称
    *   资源描述
    *   资源作者（上传者的钱包地址）
    *   IPFS 哈希（指向链下加密文件的地址）
*   **权限管理**：
    *   谁有权访问这个资源（一个地址列表）。
    *   资源的访问价格（例如 0.01 ETH 或 10 个平台代币）。
*   **激励与分配**：
    *   当有人支付费用获取访问权限时，智能合约自动将费用转给资源作者。

#### **Solidity 智能合约（简化示例）**

下面是一个极简的 Solidity 合约框架，用来演示核心思想。

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract CourseSharing {

    // 定义一个结构体来存储课程资源的信息
    struct Resource {
        uint id;                // 唯一ID
        string name;            // 资源名称
        string ipfsHash;        // 存储在IPFS上的加密文件哈希
        address payable owner;  // 资源所有者地址（可接收付款）
        uint price;             // 访问资源的价格（以wei为单位, 1 ETH = 1e18 wei）
    }

    // 存储所有资源
    mapping(uint => Resource) public resources;
    uint public resourceCount = 0;

    // 记录每个用户对每个资源的访问权限
    mapping(uint => mapping(address => bool)) public accessPermissions;

    // 事件：当新资源被添加时触发，方便前端监听
    event ResourceAdded(uint id, string name, address owner);
    // 事件：当有用户购买访问权限时触发
    event AccessGranted(uint resourceId, address user);

    /**
     * @dev 允许用户上传一个新资源
     * @param _name 资源名称
     * @param _ipfsHash 资源的IPFS哈希
     * @param _price 资源访问价格
     */
    function addResource(string memory _name, string memory _ipfsHash, uint _price) public {
        resourceCount++;
        resources[resourceCount] = Resource(resourceCount, _name, _ipfsHash, payable(msg.sender), _price);
        // 上传者默认拥有对自己资源的访问权限
        accessPermissions[resourceCount][msg.sender] = true;
        emit ResourceAdded(resourceCount, _name, msg.sender);
    }

    /**
     * @dev 允许用户支付费用来获取资源访问权限
     * @param _resourceId 想要访问的资源ID
     */
    function grantAccess(uint _resourceId) public payable {
        // 检查资源是否存在
        require(_resourceId > 0 && _resourceId <= resourceCount, "Resource not found");
        
        Resource storage resource = resources[_resourceId];

        // 检查支付的费用是否等于设定的价格
        require(msg.value == resource.price, "Please pay the correct price");

        // 将费用转给资源所有者（实现公平分配）
        resource.owner.transfer(msg.value);

        // 授予调用者访问权限
        accessPermissions[_resourceId][msg.sender] = true;
        emit AccessGranted(_resourceId, msg.sender);
    }

    /**
     * @dev 检查用户是否拥有访问特定资源的权限（这是一个只读函数，不消耗Gas）
     * @param _resourceId 资源ID
     * @param _user 用户地址
     * @return bool 是否有权限
     */
    function hasAccess(uint _resourceId, address _user) public view returns (bool) {
        return accessPermissions[_resourceId][_user];
    }
}
```

### 4. 如何满足所有作业要求

*   **去中心化平台**：✓ 核心逻辑通过部署在以太坊等公链上的智能合约实现，不受任何单点控制。
*   **区块链和智能合约**：✓ 方案完全基于 Solidity 智能合约。
*   **激励用户贡献资源**：✓ `grantAccess` 函数确保每次资源被有偿访问时，费用会自动转给资源贡献者（`owner`），形成了直接的经济激励。
*   **版权追溯**：✓ 每次 `addResource` 的调用都记录在区块链上，交易的发起者 `msg.sender` 就是作者，这个记录是公开、透明且不可篡改的。
*   **公平分配**：✓ `resource.owner.transfer(msg.value)` 这行代码实现了收益的自动、公平分配，无需第三方介入。
*   **管理资源访问权限**：✓ `accessPermissions` 这个映射变量和 `hasAccess` 函数共同实现了精细化的权限管理。
*   **保护用户隐私**：✓ 方案通过**链下加密**文件，**链上只存储加密文件的地址**来实现隐私保护。智能合约本身不存储任何敏感的课程内容，只负责人、财、权的确认。解密的密钥可以通过点对点的方式安全地分享给已付费的用户，这一步可在链下完成。

这个方案足够简单，重点突出了区块链和智能合约的价值，是完成这个作业的理想切入点。